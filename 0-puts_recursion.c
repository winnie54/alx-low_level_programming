#include  "main.h"

/**
 * _puts_string - prints a string followed by a new line
 * @s: string to be printed
 * Returns: 0
 */
void _puts_string(char *s)
{
	char s;

	for (s = "puts with recursion")

	return (s);
}
/* main	- calls the function _puts_string */
int main(void)
{
	_putchar("%s\n", _puts_string("puts with recursion ");

	return (0);
}
